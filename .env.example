# --- SMTP (envoi du brief par email) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
MAIL_TO=remi@made-in-ai.fr

# --- Sécurité du webhook (optionnel) ---
TALLY_SIGNING_SECRET=

# --- Supabase (historique des exécutions) ---
SUPABASE_URL=https://fbywkloqrxcdxrqbahxr.supabase.co
# Settings -> API -> "service_role" (clé secrète, serveur uniquement)
SUPABASE_SERVICE_ROLE_KEY=

# --- Dashboard : authentification Google (restreinte au domaine) ---
# Google Cloud Console -> Credentials -> OAuth client ID (Web application)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Domaine autorisé pour la connexion
ALLOWED_DOMAIN=made-in-ai.fr
# Secret pour signer les cookies de session (chaîne aléatoire longue)
SESSION_SECRET=
# (optionnel) Force l'URL de base pour les redirections OAuth, ex. https://automatisation-six.vercel.app
AUTH_BASE_URL=
